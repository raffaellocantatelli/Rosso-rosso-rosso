# Stato del progetto — rosso-rosso-rosso

> Prima di lavorare su questo repo: leggi questo file.
> Prima di chiudere la sessione: aggiornalo — data, cosa hai fatto, cosa resta aperto.
>
> Non è un archivio filosofico, è un promemoria pratico. Se una voce qui sotto
> non è più vera, correggila invece di aggiungerne una nuova sopra.

## Ultimo aggiornamento

2026-08-04 — creazione di questo file, nessuna modifica al codice esistente.

## Stato attuale

Repo con due sistemi indipendenti (vedi `README.md` per i dettagli):

- **SDQ-1** (`sdq1/`) — pipeline di 6 agenti (RAFFA-001, DECOMP-005, MEMO-002,
  SENTIN-004, GEN-006, WAVE-003), router multi-provider con circuit breaker,
  Vector Store, SAR (auto-riflessione), watchdog.
- **R³∞** (`registro_ipotesi.py`) — framework epistemologico: P5 (niente
  auto-conferma), P6 (serve la contro-forza / criterio di falsificazione
  dichiarato prima, altrimenti l'ipotesi non può mai essere confermata).
  Registro ipotesi in `registro_ipotesi.json` (H1, H2, H3 — contenuto privato,
  non toccato da questo aggiornamento).
- **r3/** — sistema di ridondanza documentale, indipendente dal registro
  ipotesi nonostante il nome simile.

## Decisioni prese

- I principi P5/P6 sono stati esportati, in forma pubblica e generica, verso
  il sito `claudioterzi` (mazzo R³∞ dei Tarocchi Quantici, capitolo dedicato
  nel Libro). **Le ipotesi H1/H2 di questo registro non sono state esposte**
  — riguardano persone reali e restano qui.
- Non è stato modificato nessun file esistente di questo repo in questa
  sessione: solo aggiunti `CLAUDE.md` e questo file di stato.

## Aperto / prossimi passi

- Nessuna richiesta pendente al momento di questo aggiornamento.

## Repo collegati

- **claudioterzi** (pubblico) — https://github.com/raffaellocantatelli/claudioterzi
  Il sito che porta al pubblico, in forma onesta e senza esporre dati privati,
  il principio P5/P6 di questo repo.
