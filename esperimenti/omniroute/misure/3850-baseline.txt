DELTA Candidate A — misurato in sessione: delta-3850-baseline
immagine: diegosouzapw/omniroute:3.8.50
---------------------------------------------------------------------
A1 catalogo raggiungibile prima e dopo                   PASS    HTTP prima=200 dopo=200; 315 modelli prima
A2 denylist riletta dallo stato, non solo accettata      FAIL    PATCH HTTP 200; riletta 0/5 voci
A3 ogni voce, da sola, fa sparire almeno un id           FAIL    0/5 voci efficaci; 0 id tolti in tutto
A4 la denylist intera toglie quello che tolgono le voci  PASS    rimasti in vetrina: nessuno
A5 e non toglie nient'altro                              PASS    spariti senza una voce che li spieghi: nessuno
A6 auto risponde e non sceglie un modello tolto          UNKNOWN auto non rispondeva gia' prima del PATCH (HTTP 500 -> 500): nessun provider configurato, non misurabile qui
A7 il pool di auto/* si muove solo per la denylist       PASS    poolSize 13 -> 13 (0 modelli tolti dalla vetrina)
---------------------------------------------------------------------

Effetto misurato di ogni voce, applicata da sola:
  kilo-gateway/anthropic/claude-opus-5       NIENTE — il provider 'kilo-gateway' non compare in questo catalogo: qui non c'e' niente da togliere. Rimisurala dove e' configurato
  opencode/big-pickle                        NIENTE — nessun modello sparisce quando si applica questa voce da sola
  oc/big-pickle                              NIENTE — scritta con l'alias; il predicato riceve l'id canonico 'opencode' — prova 'opencode/big-pickle'
  felo/felo-chat                             NIENTE — nessun modello sparisce quando si applica questa voce da sola
  felo/felo-search                           NIENTE — nessun modello sparisce quando si applica questa voce da sola

E1  il modello negato chiamato per nome NON ha risposto (HTTP 401).
    Non deducibile dalla denylist: puo' essere un provider assente o senza
    credenziali. Serve un'istanza con quel provider configurato.

VERDETTO: REJECT A — A2, A3
